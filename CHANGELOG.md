# Changelog

## 1.3.0-beta.5 (unreleased)

- Minecraft 26.3 Snapshot 9–10 및 Pre-release 1–2 대상 추가
- Fabric이 시험판 버전을 정규화하는 방식에 맞춰 지원 선언 수정: 시험판에서 버전 불일치로 실행이 거부되는 문제 해결
- 창작 인벤토리와 조합법 검색에서 Shift 쌍자음·모음 입력을 보존하고 대소문자만 바뀐 질의도 갱신
- 두벌식 검색 변환 중 보조 평면 유니코드 문자가 다른 글자로 변환되는 오류 수정
- F6 비상 모드를 현재 단일행 입력창에 한정하고 포커스 이동·화면 닫기 때 자동 해제
- 입력창 글자 수 제한이나 필터가 입력을 거절하면 비상 조합 상태를 정리하도록 보강
- 조합 내용이 바뀌지 않은 책 입력의 표시용 문자열·줄바꿈 결과 재사용, 글꼴 리소스 재로딩 시 캐시 무효화
- 단일행·책·표지판 렌더링 중 예외가 발생해도 원래 입력값과 커서 복원
- 개발 기준을 Fabric Loader 0.19.5 / Loom 1.17.20으로 갱신, 최소 Loader 요구는 0.19.3 유지
- 한글 11,172음절 회귀 검사와 동일 배포 JAR의 버전별 Mixin·입력 위젯 검증 자동화

## 1.3.0-beta.4

- Minecraft 26.1, 26.1.1, 26.1.2, 26.2 및 26.3 Snapshot 1–8 호환 빌드 추가
- Snapshot 4의 SDL3 입력 전환에서도 F6·Backspace·물리 키 두벌식 입력이 유지되도록 입력 상수 호환 계층 추가
- Minecraft 26.1과 26.2 사이의 화면·오버레이 API 차이를 작은 UI 호환 계층으로 분리
- 전체 12개 대상의 클린 빌드·Mixin 리맵·조합기 회귀 테스트를 한 번에 수행하는 검증 스크립트 추가
- 프로젝트 설명과 검색 예시를 한국어 중심의 독자적인 문안으로 정리

## 1.3.0-beta.3

- 공식 영문 표기에 맞춰 프로젝트 이름, 모드 ID, 패키지, Mixin 접두사와 산출물 이름을 `Hangul`로 통일
- `Hangeul`/`hangeul`을 검색 및 이전 모드 ID 호환용 별칭으로 등록
- 사용자 제공 PLAYCITY 브랜드 이미지를 모드 아이콘으로 추가
- 아이콘 원본과 배포 리소스의 SHA-256 출처 기록 추가

## 1.3.0-beta.2

- 일반 영문 명령어에서는 한글 추천 비동기 후처리를 만들지 않도록 빠른 경로 추가
- 검색 질의에서 불필요한 정규식 분할·중간 목록·중복 제거 컬렉션 생성을 제거
- 한글이 없는 문자열은 두벌식·역자판 변환용 조합기와 버퍼 생성을 건너뛰도록 최적화
- F6 비상 조합기의 키 분류 객체와 겹받침 분리 배열 할당 제거
- 공개 저장소용 아키텍처·기여·릴리스 검증 문서 추가

## 1.3.0-beta.1

- 창작 인벤토리·조합법 책 이름 검색에 초성 색인 추가
- `rmarhl` → `금괴` 형태의 영문 두벌식 자판 검색 복구 추가
- 전체 문구 검색이 없을 때 공백 단위 검색어를 교집합으로 찾는 다중 토큰 폴백 추가
- IME 조합 문자열을 실제 위젯 값에 저장하지 않고 검색 결과에 즉시 반영
- 한글 상태로 영문 명령어의 물리 키를 누른 경우 서버가 허용한 Brigadier 리터럴을 Tab 후보로 복구
- 고정 사전 없이 한·영 자음 발음을 비교해 `디버그`·`메시지`·`게임모드` 같은 음차 명령을 검색
- 검색 색인을 언어/검색 트리 재생성 시 한 번만 만들고, 프레임·틱 단위 전체 목록 순회를 피하도록 최적화
- 초성, 자판 양방향 변환, 음차 발음 키, 검색 결과 순서, Brigadier 명령어 복구 회귀 테스트 추가

## 1.2.0-beta.1

- 프로젝트 이름·모드 ID·패키지·산출물의 초기 브랜드 구조 정리
- 책과 깃펜 등 `MultiLineEditBox` 기반 다중행 입력에서 인라인 IME 조합 지원
- 표지판의 `TextFieldHelper` 입력 및 줄별 커서 위치에서 인라인 조합 지원
- 명령어 블록 입력이 단일행 공통 경로로 지원됨을 검증
- 분리된 바닐라 preedit 팝업 대신 운영체제 한자 후보창을 실제 인라인 커서에 고정
- 조합 중 렌더 값과 실제 저장·패킷 값을 분리해 미완성 문자열 전송 방지
- 줄바꿈과 여러 줄 선택 치환에 대한 회귀 테스트 추가

## 1.1.0

- 기본 모드를 OS IME 자동 추종으로 변경
- 조합 문자열을 실제 입력값 변경 없이 커서 위치에 인라인 렌더링
- 오른쪽 Alt/한영키 가로채기 제거
- F6을 비상용 강제 한글 모드 전환으로 변경
- 선택 영역, 조합 caret, 화면 스크롤 렌더 처리 추가
- 3단 겹모음의 단계별 Backspace 이력 보존
- 인라인 preedit 텍스트 변환 회귀 테스트 추가

## 1.0.2

- `EditBox` 물리 키 입력 훅 추가
- 포커스 판정 실패 시 F6 전환이 무시되던 문제 수정

## 1.0.1

- OS 문자 확정을 기다리지 않는 물리 키 두벌식 조합 추가
- F6 전환키와 native preedit 억제 추가

## 1.0.0

- 최초 Fabric 26.2 빌드
