# Contributing

## 코드와 자산의 출처

- 기여 코드는 직접 작성한 것이어야 합니다.
- 다른 모드의 소스·번역표·리소스·스크린샷을 복사해 제출하지 마십시오.
- 외부 코드나 자산이 꼭 필요하면 출처, 라이선스, 수정 여부를 먼저 명시하고 저장소 라이선스와 배포 조건을 검토해야 합니다.
- 새 라이브러리를 JAR에 포함하기 전에 재배포 조건과 고지 의무를 확인해야 합니다.
- 프로젝트 아이콘과 홍보 이미지는 원본 작업물만 사용합니다. Minecraft 로고, 공식 글꼴을 모사한 글자, 게임 텍스처·아이템·블록 이미지, 다른 모드의 표장을 로고 구성 요소로 사용하지 않습니다.
- 바이너리 파일을 추가할 때는 원본 제작 파일, 제작자, 사용 권한을 확인할 수 있어야 합니다.

공개된 다른 제품의 동작을 요구사항으로 검토할 수는 있지만, 구현은 이 저장소의 구조와 테스트를 기준으로 독립적으로 작성합니다.

## 구현 원칙

- 조합 중 문자열과 실제 저장·전송 값의 분리를 유지합니다.
- 매 프레임 또는 매 틱 전체 아이템·조합법·명령어 목록을 순회하지 않습니다.
- 일반 영문 입력 경로에는 한글 전용 컬렉션이나 비동기 후처리를 추가하지 않습니다.
- 새 믹스인은 정확한 Minecraft 버전의 메서드 서명과 주입 횟수를 검증합니다.
- 선택 기능이 실패하더라도 채팅 화면과 바닐라 입력은 계속 동작해야 합니다.

## 검증

Windows PowerShell에서 다음 명령을 실행합니다.

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\build.ps1
```

변경한 입력창에서 조합·확정·Backspace·방향키·선택 교체를 확인하고, 검색 변경에는 초성·두벌식·다중 단어·일반 영문 질의 회귀 검사를 포함합니다.
